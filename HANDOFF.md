# Handoff

Status: **NOT SAFE FOR LUNA HANDOFF**

Terra may change this status only after all of the following are committed:

- exact finite domain and mechanism specification are frozen;
- success criteria and verifier semantics are frozen;
- automated tests pass;
- a canonical baseline is reproduced with recorded commands and outputs;
- `PROJECT_CHARTER.md`, `PREREGISTRATION.md`, `MECHANISM_SPEC.md`, and `STATUS.md` are current;
- the remaining work is a bounded execution queue that cannot change the above.

When the gate is satisfied, replace the status with **SAFE FOR LUNA HANDOFF** and record:

1. commit SHA;
2. exact test and run commands;
3. frozen configuration and seed paths;
4. expected output paths;
5. an explicit, bounded Luna task list;
6. escalation conditions requiring Terra.

Luna must not modify frozen specifications, verifier semantics, acceptance criteria, or research conclusions.
